/** @type {import('next').NextConfig} */
const nextConfig = {
    sassOptions: {
        includePaths: ['app']
    },
    images: {
        domains: ['img.icons8.com'],
      },
}

module.exports = nextConfig
